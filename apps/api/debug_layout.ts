import { readFileSync } from 'fs';
import { execSync } from 'child_process';

try {
  // Let's just do a quick curl with a manually generated token!
  // I will generate a token for jon@snow.com
} catch (e) {
}
